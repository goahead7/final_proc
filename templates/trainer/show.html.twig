{% extends 'base.html.twig' %}

{% block title %}Trainer{% endblock %}

{% block body %}
    <h1>Trainer</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ trainer.id }}</td>
            </tr>
            <tr>
                <th>FIO</th>
                <td>{{ trainer.FIO }}</td>
            </tr>
            <tr>
                <th>Qualification</th>
                <td>{{ trainer.qualification }}</td>
            </tr>
            <tr>
                <th>City</th>
                <td>{{ trainer.city }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('trainer_index') }}">back to list</a>

    <a href="{{ path('trainer_edit', {'id': trainer.id}) }}">edit</a>

    {{ include('trainer/_delete_form.html.twig') }}
{% endblock %}
